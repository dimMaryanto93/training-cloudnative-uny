stages:
    - test

variables:
    DOCKER_IMAGE: private.nexus-registry.docker.local:8086/alpine

print-env:
    image: $DOCKER_IMAGE
    script: "echo 'Hi this is from gitlab ci with env: $AWS_CRED'"